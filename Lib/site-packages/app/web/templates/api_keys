<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SEGOTEL - {% block title %}{% endblock %}</title>
    <link rel="stylesheet" href="{{ url_for('static', filename='css/style.css') }}">
</head>
<h2>Manage API Keys</h2>

<h3>Your API Keys</h3>
<ul>
    {% for key in api_keys %}
        <li>
            {{ key.api_key }} 
            - Status: {{ 'Active' if key.is_active else 'Inactive' }}
            {% if key.is_active %}
            <form method="POST" action="{{ url_for('customer.deactivate_api_key', api_key_id=key.id) }}">
                <button type="submit">Deactivate</button>
            </form>
            {% endif %}
        </li>
    {% endfor %}
</ul>

<a href="{{ url_for('customer.generate_api_key') }}">Generate New API Key</a>
{% endblock %}
